namespace Athkar.Shareds.Enums;

/// <summary>Where an anonymous install is running. Drives push payload shape and store-level reporting.</summary>
public enum DevicePlatform
{
    Unknown = 0,
    Android = 1,
    Ios = 2,
    Web = 3,
}
