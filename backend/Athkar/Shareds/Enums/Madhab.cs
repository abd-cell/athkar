namespace Athkar.Shareds.Enums;

/// <summary>
/// Which shadow length starts Asr. The only fiqh choice prayer times need, and
/// the one users most often come looking for in settings.
/// </summary>
public enum Madhab
{
    /// <summary>Shadow equal to the object — the majority position.</summary>
    Standard = 1,

    /// <summary>Shadow twice the object.</summary>
    Hanafi = 2,
}
