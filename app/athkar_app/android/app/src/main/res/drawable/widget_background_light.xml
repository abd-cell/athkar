<?xml version="1.0" encoding="utf-8"?>
<!-- Outlined parchment, matching the app's cards: no shadow, a hairline border. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/widget_paper_light" />
    <corners android:radius="18dp" />
    <stroke android:width="1dp" android:color="@color/widget_hairline_light" />
</shape>
